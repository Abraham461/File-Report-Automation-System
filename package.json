{
  "name": "file-report-automation-system",
  "version": "1.0.0",
  "description": "Report generation module with templates, exports, and frontend flow",
  "main": "app/main.py",
  "type": "module",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 3000",
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 3000",
    "test": "node --test"
  }
}
